# Redis configuration for grpc-chat

# Enable AOF persistence
appendonly yes
appendfsync everysec

# Disable protected mode since we're in Docker
protected-mode no

# ACL configuration
# Disable default user
user default off

# Create dedicated redis user with password
user redis on >redis ~* &* +@all

